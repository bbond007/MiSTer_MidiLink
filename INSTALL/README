How to install:

   -  upload (via WinSCP) MidiLink.tar.gz to /media/fat
   -  conenct to MiSTer via SSH (Putty)
   -  #cd /
   -  #uartmode 0
   -  #tar -xvzf /media/fat/MidiLink.tar.gz
   -  delete /media/fat/MiSTer
   -  upload new MiSTer to /media/fat
   -  upload zImage_dtb to /media/fat/linux <-- required for MUNT/FluidSynth 

Post install:
   
   -  upload FluidSynth soundfont ".sf2" file to /meda/fat/soundfonts/default.sf2

        -  https://www.youtube.com/watch?v=_J8-CgvC-As <-- Sounfont I'm using. 
   
   -  upload MT-32 or CM-32 ROM files to /media/fat/mt32-rom-data - these are 
      property of Roland and I can't provide them.

        suggested (for Roland Computer Music module): 

        -  CM32L_CONTROL.ROM
        -  CM32L_PCM.ROM
        
        or (for Roland MT-32 Multi-Timbre Sound Module) : 

        -  CM32L_CONTROL.ROM
        -  CM32L_PCM.ROM

   -  look over /media/fat/config/MidiLink.INI and make any necessary changes.
          
What is in "MidiLink.tar.gz"?

    -  /sbin/midilink                     
    -  /sbin/uartmode                             (Update to existing script)
    -  /sbin/fluidsynth                           (FluidSynth ALSA daemon)
    -  /sbin/mt32d                                (MUNT ALSA daemon)
    -  /usr/lib/libasound.so                      (symlink libasound.so.2.0.0)
    -  /usr/lib/libasound.so.2                    (symlink libasound.so.2.0.0)
    -  /usr/lib/libasound.so.2.0.0                (Required for MUNT and FluidSynth)
    -  /usr/lib/libfluidsynth.so                  (symlink libfluidsynth.so.2.0.1)
    -  /usr/lib/libfluidsynth.so.2                (symlink libfluidsynth.so.2.0.1)
    -  /usr/lib/libfluidsynth.so.2.0.1            (Required for FluidSynth) 
    -  /usr/lib/libjack.so                        (symlink libjack.so.0.0.28)
    -  /usr/lib/libjack.so.0                      (symlink libjack.so.0.0.28)
    -  /usr/lib/libjack.so.0.0.28                 (Required for FluidSynth)
    -  /lib/modules/4.5.0-socfpga-r1/snd-dummy.ko (Support for use without USB audio adapter) 
    -  /bin/aconnect                              (*not required - diagnostic tool)
    -  /bin/amidi                                 (*not required - diagnostic tool)
    -  /bin/amixer                                (Used by MidiLink)  
    -  /bin/aplay                                 (*not required - diagnostic tool)
    -  /bin/aplaymidi                             (*not required - diagnostic tool)
    -  /usr/share/alsa/alsa.conf                  (Required for ALSA)
    -  /usr/share/alsa/pcm/default.conf           (Required for ALSA)
    -  /usr/share/alsa/pcm/dsnoop.conf            (Required for ALSA)
    -  /usr/share/alsa/pcm/dmix.conf              (Required for ALSA)
    -  /usr/share/alsa/cards/aliases.conf         (Required for ALSA)
    -  /media/fat/config/MidiLink.INI             (Required for MidiLink)
    -  /media/fat/soundfonts/README               (not required / placeholder for soundfonts)
    -  /media/fat/mt32-rom-data/README            (not required / placeholder for MUNT ROMS)
         
        
         
            
