How to install:

   -  upload (via WinSCP) MidiLink.tar.gz to /media/fat
   -  conenct to MiSTer via SSH (Putty)
   -  #cd /
   -  #uartmode 0
   -  #tar -xvzf /media/fat/MidiLink.tar.gz
   -  delete /media/fat/MiSTer
   -  upload new MiSTer to /media/fat
   -  upload zImage_dtb to /media/fat/linux <-- required for MUNT/FluidSynth 

Post install:
   
   -  upload FluidSynth soundfont ".sf2" file to /meda/fat/soundfonts/default.sf2
   
   -  upload MT-32 or CM-32 ROM files to /media/fat/mt32-rom-data

        suggested (for Roland Computer Music module): 

        -  CM32L_CONTROL.ROM
        -  CM32L_PCM.ROM
        
        or (for Roland MT-32 Multi-Timbre Sound Module) : 

        -  CM32L_CONTROL.ROM
        -  CM32L_PCM.ROM

   -  look over /media/fat/config/MidiLink.INI and make any necessary changes.
          
What is in "MidiLink.tar.gz"?

    -  sbin/midilink
    -  sbin/uartmode
    -  sbin/fluidsynth
    -  sbin/mt32d
    -  usr/lib/libasound.so              (simlink libasound.so.2.0.0)
    -  usr/lib/libasound.so.2            (simlink libasound.so.2.0.0)
    -  usr/lib/libasound.so.2.0.0
    -  usr/lib/libfluidsynth.so          (simlink libfluidsynth.so.2.0.1)
    -  usr/lib/libfluidsynth.so.2        (simlink libfluidsynth.so.2.0.1)
    -  usr/lib/libfluidsynth.so.2.0.1
    -  usr/lib/libjack.so                (simlink libjack.so.0.0.28)
    -  usr/lib/libjack.so.0              (simlink libjack.so.0.0.28)
    -  usr/lib/libjack.so.0.0.28
    -  bin/aconnect                      (not required used for testing)
    -  bin/amidi                         (not required used for testing)
    -  bin/amixer
    -  bin/aplay                         (not required used for testing)
    -  bin/aplaymidi                     (not required used for testing)
    -  usr/share/alsa/
    -  usr/share/alsa/alsa.conf
    -  usr/share/alsa/pcm/
    -  usr/share/alsa/pcm/default.conf
    -  usr/share/alsa/pcm/dsnoop.conf
    -  usr/share/alsa/pcm/dmix.conf
    -  usr/share/alsa/cards/
    -  usr/share/alsa/cards/aliases.conf
    -  media/fat/config/MidiLink.INI
    -  media/fat/soundfonts/README       (not required / information only)
    -  media/fat/mt32-rom-data/README    (not required / information only)
         
        
         
            
